name: 'olist_analytics'
version: '1.0.0'
config-version: 2

profile: 'olist_analytics'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  olist_analytics:
    staging:
      +materialized: view # Staging geralmente é view para economizar espaço
    marts:
      +materialized: table # Marts (Fatos/Dimensões) salvos como tabelas físicas